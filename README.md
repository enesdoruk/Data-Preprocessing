# Data-Preprocessing

import library
import data
iloc,loc , row, column
object,dataframe
column names
add feature drop feature
concat
standard scaler
label encoder
onehotencoder
missing value find and remove or add 
normalization
splitting dataset
